# BioData Chat Requirements
# Core dependencies for CLI interface
click>=8.0.0
rich>=13.0.0
requests>=2.28.0

# LLM and MCP dependencies
ollama
fastmcp

# Scientific database libraries
# Note: Install bananompy if available, fallback to requests for API calls
bananompy; python_version>="3.8"

# Type hints and async support
typing-extensions>=4.0.0; python_version<"3.11"
aiohttp>=3.8.0
